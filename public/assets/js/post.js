(async()=>{const id=getParam('id');const wrap=document.querySelector('#postWrap');if(!id){wrap.innerHTML='IDなし';return;}
const posts=await loadPosts();const p=posts.find(x=>x.id===id);if(!p){wrap.innerHTML='見つかりません';return;}
wrap.innerHTML=`<article class="card"><div class="meta"><span>${fmtMonth(p.date)}</span></div><h1 class="h1">${p.title}</h1><div class="article">${escapeHtml(p.body)}</div><div class="notice"><a href="./index.html">← 一覧へ</a></div></article>`;})();